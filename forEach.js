const prices = [5, 10, 20];
const updatedPrices = prices.map(price => price + 2);
console.log(updatedPrices);

const arr1 = ["a", "b", "c", "d", "e"];
const uppercase = arr1.map(element => element.toUpperCase());
console.log(uppercase);

